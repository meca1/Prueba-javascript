import {cleanConsole, createAll} from './data';
const companies = createAll();

cleanConsole(2, companies);
console.log('---- EXAMPLE 2 --- ', 'Put here your function');

// Create a function which takes the variable "companies" as a parameter
// and a 'hasCar' boolean. Keep in all companies only users
// whose value of the 'car' attribute is equal to the 'hasCar' parameter
// then modify the 'usersLength' attribute of the companies so that it is
// equal to the number of users who have been kept


// Créer une fonction qui qui prend en paramètre la variable "companies"
// et un boolean 'hasCar'. Garder dans toutes les companies uniquement les users
// dont la valeur de l'attribut 'car' est  égale au paramètre 'hascar'
// puis modifier l'attribut 'usersLength' des companies pour qu'il soit
// égal au nombre d'user qui ont été gardés


// Créer une fonction prenant en paramètre la variable "companies" et le
// booléen "hasCar". Pour chaque "companies" vous devez garder uniquement les
// "users" dont la valeur de l'attribut "car" est égal au paramètre de la
// fonction "hasCar" et l'attribut "usersLength" doit renseigner le nombre de
// "users" correspondant au paramètre "hasCar".
