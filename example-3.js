import {cleanConsole, createAll} from './data';

const companies = createAll();

cleanConsole(3, companies);
console.log('---- EXAMPLE 3 --- ', 'Put here your function');

// Create a function that takes the variable "companies" as a parameter and
// which returns a boolean validating the fact that all the names of the companies
// and the users' firstName and lastName attributes are capitalized
//
// test the proper functioning of this function by importing the function
// performed in example 1


// Créer une fonction qui qui prend en paramètre la variable "companies" et
// qui renvoie un boolean validant le fait que toutes les noms des companies
// et les attributs firstName et lastName des users sont bien en majuscule
// tester le bon fonctionnement de cette fonction en important la fonction
// réalisée à l'exercice 1

// Créer une fonction prenant en paramètre la variable "companies" et renvoyant
// un booléen validant que tous les noms des compagnies et les attributs firstName
// et lastName des "users" sont en majuscule. Vous devez tester le fonctionnement
// de cette fonction en important la fonction créée pour l'exemple 1.
